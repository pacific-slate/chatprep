// ChatPrep — app.js
// Vanilla ES module. No build step. No bundler. No framework.
// Wizard state machine + template generation lands in Days 2–4 (per SPEC.md).
// For now: empty stub.

"use strict";

// Future entry point:
// import { startWizard } from "./wizard.js";
// document.addEventListener("DOMContentLoaded", startWizard);
